PLANNING_PROMPT = """
你是一个研究规划专家，负责将研究主题系统性地分解为多个子任务。

当前日期：{current_date}

研究主题：{research_topic}

## 分解原则

将主题分解为 10-20 个子任务，覆盖以下 8 个维度：
1. **基础概念** - 定义、背景、核心原理
2. **发展历程** - 历史沿革、重要里程碑、演进趋势
3. **技术实现** - 技术架构、算法原理、实现方式
4. **应用场景** - 实际用例、行业应用、典型案例
5. **生态现状** - 市场规模、主要玩家、竞争格局
6. **优劣势分析** - 优势特点、局限性、面临的挑战
7. **发展趋势** - 最新进展、未来方向、预测分析
8. **实践指南** - 如何学习/使用、工具推荐、最佳实践

## 输出要求

返回 JSON 数组，每个子任务包含：
- title：任务标题（简洁，15字以内）
- dimension：所属维度（从上述8个维度选择）
- query：搜索查询（英文，中文主题需翻译以获得更好结果）

## 示例

研究主题：Python 3.14 新特性
[
  {{"title": "Python 3.14概述", "dimension": "基础概念", "query": "Python 3.14 new features release 2024"}},
  {{"title": "性能优化改进", "dimension": "技术实现", "query": "Python 3.14 performance improvements benchmark"}},
  ...
]

请确保：
1. 覆盖所有 8 个维度（如果主题不适用某些维度可跳过）
2. 子任务之间有逻辑递进关系
3. 搜索查询简洁且能精准命中相关内容
4. 只返回 JSON 数组，不要其他文本
"""


TIER1_SEARCH_PROMPT = """你是一个资深研究专家，知识渊博。请用你的训练知识全面、深入地回答以下问题。

问题：{question}

## 回答要求

1. **全面性**：覆盖问题的所有重要方面，不遗漏关键信息
2. **深度**：提供专业、详细的解释，包含内部机制和原理
3. **准确性**：确保信息准确，标注不确定性
4. **结构性**：使用标题、列表等组织内容，便于阅读
5. **数据支撑**：包含具体数据、时间、案例等证据

## 内容组织

- 先给出核心定义和概述
- 再详细展开各个要点
- 包含正反两面观点（如有争议）
- 给出具体的例子和应用场景
- 标注信息来源（如果不确定，说"基于训练知识"）

请尽可能详尽地回答，不要省略细节。"""


EXTRACT_PROMPT = """你是一个研究分析师。请从以下内容中提取与问题相关的所有关键发现。

## 研究问题
{question}

## 内容来源
{content}

## 提取要求

1. **数量**：提取 5-10 个关键发现
2. **质量**：每个发现必须是具体的事实性陈述
3. **细节**：包含数据、时间、人名、机构名、百分比等具体信息
4. **完整**：保留所有有信息量的内容，不遗漏重要发现
5. **去重**：合并重复信息，保留最完整的版本

## 发现格式

每个发现应包含：
- 具体的事实或数据
- 明确的来源（如有）
- 与问题的关联性

## 输出格式

返回 JSON 字符串数组，每个元素是一个字符串表示一个发现。
只返回 JSON 数组，不要其他文本。

## 示例

[
  "GPT-4 发布于 2023 年 3 月，拥有 100 万亿参数（来源：OpenAI 官方博客）",
  "研究表明，RLHF 技术显著提升了模型遵循指令的能力（来源：Anthropic 论文，2022）",
  ...
]"""


GAP_ANALYSIS_PROMPT = """你是一个研究质量评估专家。请评估当前研究的完整性，并识别知识缺口。

## 研究计划（已分解的子任务）
{plan}

## 已完成发现摘要
{findings_summary}

## 当前迭代信息
- 当前迭代：{iteration}/{max_iterations}
- 剩余迭代次数：{max_iterations} - {iteration}

## 评估维度

请从以下维度评估研究完整性：
1. **覆盖度** - 是否涵盖了计划中的所有子任务？
2. **深度** - 每个发现是否足够详细和深入？
3. **准确性** - 信息是否准确可靠？
4. **时效性** - 信息是否足够新？
5. **均衡性** - 各维度是否均衡覆盖？

## 输出要求

返回 JSON 格式：
{{"gaps": ["具体缺口1", "具体缺口2", ...], "sufficient": true/false}}

说明：
- gaps：识别出的知识缺口，每个缺口应该是具体的、可搜索的问题
- sufficient：true 表示研究已充分，false 表示还有缺口需要补充

规则：
- 如果 gaps 为空数组，sufficient 必须为 true
- 如果 iteration 接近 max_iterations，即使有小缺口也返回 sufficient=true
- 缺口描述要具体，能转化为搜索查询

只返回 JSON 数组。"""


REPORT_PROMPT = """
你是报告撰写专家。请整合所有研究发现，生成一份专业、详尽的研究报告。

## 研究主题
{research_topic}

## 研究计划
{plan}

## 所有发现
{findings}

## 报告结构

请按以下结构撰写报告：

# {研究主题}

## 一、概述
- 研究背景和目的
- 研究方法和范围
- 主要发现摘要（3-5个核心观点）

## 二、基础概念
- 核心定义
- 背景和起源
- 关键术语解释

## 三、详细分析
- 按逻辑顺序深入展开
- 每个部分包含具体数据、案例、证据
- 包含正反两面分析

## 四、应用与实践
- 实际应用场景
- 实践指南和建议
- 工具和资源推荐

## 五、发展趋势
- 最新进展
- 未来方向
- 预测分析

## 六、总结与建议
- 主要结论
- 核心洞见
- 行动建议

## 七、参考文献
- 所有使用的信息来源
- 按引用顺序编号

## 写作要求

1. **详尽**：6000-10000 字，内容充实
2. **专业**：使用专业术语，保持学术严谨性
3. **结构清晰**：层次分明，便于阅读
4. **有据可查**：每个观点尽量有数据或案例支撑
5. **格式规范**：正确使用 Markdown 格式
"""
