PLANNING_PROMPT = """你是一个资深研究规划专家。请将以下研究主题进行全方位、系统性的分解，确保覆盖所有重要维度。

研究主题: {topic}

你必须覆盖以下维度（每个维度至少 1-2 个子问题）：
1. 背景与定义：核心概念、术语界定、发展历程
2. 现状分析：当前规模、主要参与者、市场份额、技术水平
3. 技术/方法论：关键技术和方法的深入探讨
4. 应用与案例：实际应用场景、典型案例
5. 挑战与争议：存在的问题、争议点、瓶颈
6. 趋势与展望：未来发展方向、新兴趋势、潜在突破
7. 影响与意义：对社会、经济、产业的影响
8. 对比分析：不同方案/国家/时期的对比（如适用）

每个子问题要求：
- 具体、可研究、有明确答案范围
- 覆盖面要广，不要遗漏重要方面
- 尽可能多分解，通常 10-20 个子问题

返回 JSON 字符串数组，如 ["子问题1", "子问题2", ...]

只返回 JSON 数组，不要包含其他内容。"""


TIER1_SEARCH_PROMPT = """你是一个资深研究助手。请用你的知识全面、深入地回答以下问题。尽可能详尽。

问题: {question}

要求：
- 包含关键事实、数据、时间节点、重要人物/组织
- 多角度分析，不要只给一个维度的信息
- 如果有争议或不同观点，一并列出
- 尽可能结构化，便于提取发现"""


SEARCH_QUERY_PROMPT = """你是一个搜索专家。请为以下研究问题生成 2-3 个搜索引擎查询关键词。

研究问题: {question}

要求：
- 每个查询应该是简短的关键词组合（5-10 词）
- 返回 JSON 字符串数组

只返回 JSON 数组。"""


EXTRACT_PROMPT = """你是一个研究分析师。请从以下内容中提取与问题相关的关键发现，尽可能详尽。

研究问题: {question}
内容来源:
{content}

要求：
1. 提取 5-10 个关键发现，内容越多越好
2. 每个发现应该是具体的事实性陈述，包含数据、时间、人名等细节
3. 保留所有有信息量的内容，不要遗漏重要信息
4. 返回 JSON 字符串数组

只返回 JSON 数组。"""


GAP_ANALYSIS_PROMPT = """你是一个研究质量评估专家。请评估当前研究的完整性。

研究计划: {plan}
已完成问题的发现: {findings_summary}
当前迭代: {iteration}/{max_iterations}

评估：
1. 当前发现是否充分覆盖了研究计划的各个方面？
2. 哪些维度还需要进一步研究？

返回 JSON:
{{"gaps": ["具体缺口1", "具体缺口2", ...], "sufficient": true/false}}

如果 sufficient 为 true，gaps 应为空数组。只返回 JSON。"""


REPORT_PROMPT = """你是一个深度研究报告撰写专家。请基于研究发现生成一份结构化的 Markdown 研究报告。

研究主题: {topic}
研究计划: {plan}
研究发现: {findings}

报告结构：
1. ## 摘要 —— 概述研究目的和核心结论
2. ## 研究问题与发现 —— 每个问题单独一节，包含关键发现和来源
3. ## 综合分析 —— 跨问题的整体洞察
4. ## 局限性与展望
5. ## 参考资料

要求：
- 专业、客观的风格
- 发现需标注来源
- 总长度 6000-10000 字
- Markdown 格式"""
